# =============================================================================
# Copy to .env and fill in real values:
#   cp .env.example .env
#
# .env is gitignored
# =============================================================================

# BitSong validator mnemonic (coin-type 639 — derives bitsong1... address)
VAL_MNEMONIC="your 24-word mnemonic here"

# EVM / Hyperlane agent keys (hex, 0x-prefixed)
HYP_KEY=0x...
VALIDATOR_KEY=0x...
COSMOS_SIGNER_KEY=$VALIDATOR_KEY
EVM_RELAYER_KEY=$HYP_KEY
